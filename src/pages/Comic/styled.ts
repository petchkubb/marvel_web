import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
	image: {
		objectFit: 'cover',
		width: '45vw',
		height: '45vh',
		borderRadius: '8px'
	},
})

export default useStyles
